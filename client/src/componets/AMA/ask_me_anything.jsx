import React, { useState , useHistory} from "react";

export const Ask_me_anything = (props) => {



    return(
        <div>This is a streach goal lets use a reactive 
        seach bar to plum the 5e API</div>
    )
}